/* Single source for the client version — its own module so pages can import
   it without creating a cycle through main.js. */
export const APP_VERSION = '2.0.0';
export const BUILD = 'release/horus';
